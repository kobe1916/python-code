import json
import requests

from parse_url import parse_url
from pprint import pprint#美化输出

url = "https://  "

html_str = parse_url

#json.load把json字符串转化为python类型
ret1 = json.loads(html_str)
pprint(ret1)
print(type(ret1))
